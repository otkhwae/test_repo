pipeline {
    agent any
    stages{
        stage("first stage"){
            steps{
                git credentialsId: 'github', url: 'https://github.com/otkhwae/test_repo'
            }
        }
    }
}
